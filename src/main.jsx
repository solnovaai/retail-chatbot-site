import React from 'react'
import ReactDOM from 'react-dom/client'
import RetailStore from './App'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RetailStore />
  </React.StrictMode>,
)
